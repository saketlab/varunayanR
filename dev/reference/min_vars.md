# Variables that should use minimum aggregation

These variables represent minimum values that should use min() when
aggregating.

## Usage

``` r
min_vars
```

## Format

An object of class `character` of length 4.
