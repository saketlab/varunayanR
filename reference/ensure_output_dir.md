# Create output directory if it doesn't exist

Create output directory if it doesn't exist

## Usage

``` r
ensure_output_dir(output_dir)
```

## Arguments

- output_dir:

  Path to output directory

## Value

Character string path to created/existing directory
