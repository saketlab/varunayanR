# Check IMD server connectivity

Check IMD server connectivity

## Usage

``` r
check_imd_server(base_url = "https://www.imdpune.gov.in/", timeout = 30)
```

## Arguments

- base_url:

  Base URL to test.

- timeout:

  Connection timeout in seconds.

## Value

Logical indicating if server is reachable
