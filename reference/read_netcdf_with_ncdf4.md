# Read NetCDF using ncdf4 as fallback when stars fails

Read NetCDF using ncdf4 as fallback when stars fails

## Usage

``` r
read_netcdf_with_ncdf4(netcdf_file)
```

## Arguments

- netcdf_file:

  Path to NetCDF file

## Value

data.frame with processed data
