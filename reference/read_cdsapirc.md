# Read CDS API credentials from ~/.cdsapirc file

Read CDS API credentials from ~/.cdsapirc file

## Usage

``` r
read_cdsapirc(file_path = "~/.cdsapirc")
```

## Arguments

- file_path:

  Path to .cdsapirc file (default: "~/.cdsapirc")

## Value

List with credentials or NULL
