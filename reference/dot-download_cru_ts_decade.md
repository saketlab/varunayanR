# Download and decompress a single CRU TS decade file

Download and decompress a single CRU TS decade file

## Usage

``` r
.download_cru_ts_decade(
  variable,
  decade_start,
  decade_end,
  output_dir,
  use_cache
)
```
