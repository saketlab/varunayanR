# Rate variables that should be averaged

These are rate variables that should be averaged (not summed) when
aggregating to longer time periods.

## Usage

``` r
rate_vars
```

## Format

An object of class `character` of length 84.
