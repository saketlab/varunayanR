# Variables that should use maximum aggregation

These variables represent maximum values that should use max() when
aggregating.

## Usage

``` r
max_vars
```

## Format

An object of class `character` of length 8.
